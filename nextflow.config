process {
    //executor = "slurm"
    //clusterOptions = "--output=slurm-%A.out"
    cache = "lenient"
    //maxRetries = 3
    //errorStrategy = "retry"
    publishDir = "output"
}

conda {
    enabled = true
    useMicromamba = true
}
